import { Typography, Box } from "@mui/material";

export default function AllPicksPage() {
  return (
    <Box>
      <Typography variant="body1">This page will show all players' picks and results for locked weeks.</Typography>
    </Box>
  );
}
