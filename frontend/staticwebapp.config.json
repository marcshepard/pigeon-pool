{
  "routes": [
    {
      "route": "/api/*",
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
      "allowedRoles": ["anonymous"],
      "rewrite": "https://pigeon-pool-backend-gjgcdxcvgcepbfca.westus2-01.azurewebsites.net/{*path}"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/assets/*", "/*.js", "/*.css", "/*.ico", "/*.png", "/*.svg"]
  }
}
